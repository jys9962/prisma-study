```

npm init -y

npm install prisma typescript ts-node @types/node --save-dev

npx tsc --init --strict

npx prisma init

# 데이터베이스 조회해서 스키마 생성 (schema.prisma)
npx prisma db pull


# 클라이언트 생성
npm install @prisma/client

```
